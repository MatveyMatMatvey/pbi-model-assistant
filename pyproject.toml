[project]
name = "pbi-model-assistant"
version = "0.1.0"
description = "AI-assisted semantic model editor for Power BI Desktop"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"